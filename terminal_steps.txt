# using docker container
docker image build -t yolo_backend:latest .
docker run --rm -it -p 5000:5000 yolo_backend:latest
docker image tag yolo_backend:latest yixxx/yolo_backend:first
docker image push yixxx/yolo_backend:first
docker system prune

# using local environment
pip install -r torch_requirements.txt
pip install -r requirements.txt
python app.py

# open http://localhost:5000
